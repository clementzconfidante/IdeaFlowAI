<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IdeaFlowAI - Where Ideas Transform</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: white;
      text-align: center;
      padding: 80px 20px;
    }
    h1 { font-size: 3em; margin-bottom: 0.3em; }
    p { font-size: 1.2em; color: #a8dadc; margin-bottom: 40px; }
    .btn {
      background-color: #1dd3b0;
      color: #000;
      padding: 15px 30px;
      border-radius: 30px;
      font-weight: bold;
      text-decoration: none;
      margin: 10px;
      display: inline-block;
      transition: 0.3s;
      cursor: pointer;
    }
    .btn:hover { background-color: #0ef6cc; }

    textarea {
      width: 80%;
      max-width: 600px;
      height: 120px;
      padding: 10px;
      border-radius: 10px;
      border: none;
      margin-bottom: 20px;
      font-size: 1em;
      resize: none;
    }

    .output-box {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      padding: 20px;
      width: 80%;
      max-width: 600px;
      margin: 20px auto;
      text-align: left;
      display: none;
    }
  </style>
</head>
<body>
  <h1>IdeaFlowAI</h1>
  <p>Where Creativity Meets Artificial Intelligence</p>

  <textarea id="userInput" placeholder="Paste or write your content here..."></textarea><br/>
  <button id="summarizeBtn" class="btn">Summarize Text</button>

  <div class="output-box" id="outputBox">
    <h3>Summary:</h3>
    <p id="summaryText"></p>
  </div>

  <hr style="margin: 40px auto; width: 70%; border: 1px solid #1dd3b0;">

  <div>
    <button class="btn feature-btn">Convert to Video</button>
    <button class="btn feature-btn">Voice Over</button>
    <button class="btn feature-btn">Presentation</button>
    <button class="btn feature-btn">Graphic Organizer</button>
  </div>

  <script>
    const summarizeBtn = document.getElementById("summarizeBtn");
    const outputBox = document.getElementById("outputBox");
    const summaryText = document.getElementById("summaryText");

    summarizeBtn.addEventListener("click", async () => {
      const input = document.getElementById("userInput").value.trim();
      if (!input) return alert("Please enter text to summarize.");

      summaryText.innerText = "Summarizing...";
      outputBox.style.display = "block";

      // This is a simulated AI summarization (replace with API call later)
      setTimeout(() => {
        summaryText.innerText = 
          "Summary: " + input.split(" ").slice(0, 30).join(" ") + "...";
      }, 1500);
    });

    // Placeholder alert for other features
    document.querySelectorAll(".feature-btn").forEach(button => {
      button.addEventListener("click", () => {
        const feature = button.textContent.trim();
        alert(`${feature} feature coming soon!`);
      });
    });
  </script>
</body>
</html>
